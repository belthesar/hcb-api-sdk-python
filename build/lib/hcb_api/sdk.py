# This file was generated by liblab | https://liblab.com/

from .services.cards import CardsService
from .services.checks import ChecksService
from .services.ach_transfers import AchTransfersService
from .services.invoices import InvoicesService
from .services.transfers import TransfersService
from .services.donations import DonationsService
from .services.card_charges import CardChargesService
from .services.transactions import TransactionsService
from .services.organizations import OrganizationsService
from .net.environment import Environment


class HCB_API:
    def __init__(
        self, access_token: str = None, base_url: str = Environment.DEFAULT.value
    ):
        """
        Initializes HCB_API the SDK class.
        """
        self.cards = CardsService(base_url=base_url)
        self.checks = ChecksService(base_url=base_url)
        self.ach_transfers = AchTransfersService(base_url=base_url)
        self.invoices = InvoicesService(base_url=base_url)
        self.transfers = TransfersService(base_url=base_url)
        self.donations = DonationsService(base_url=base_url)
        self.card_charges = CardChargesService(base_url=base_url)
        self.transactions = TransactionsService(base_url=base_url)
        self.organizations = OrganizationsService(base_url=base_url)
        self.set_access_token(access_token)

    def set_base_url(self, base_url):
        """
        Sets the base URL for the entire SDK.
        """
        self.cards.set_base_url(base_url)
        self.checks.set_base_url(base_url)
        self.ach_transfers.set_base_url(base_url)
        self.invoices.set_base_url(base_url)
        self.transfers.set_base_url(base_url)
        self.donations.set_base_url(base_url)
        self.card_charges.set_base_url(base_url)
        self.transactions.set_base_url(base_url)
        self.organizations.set_base_url(base_url)

        return self

    def set_access_token(self, access_token: str):
        """
        Sets the access token for the entire SDK.
        """
        self.cards.set_access_token(access_token)
        self.checks.set_access_token(access_token)
        self.ach_transfers.set_access_token(access_token)
        self.invoices.set_access_token(access_token)
        self.transfers.set_access_token(access_token)
        self.donations.set_access_token(access_token)
        self.card_charges.set_access_token(access_token)
        self.transactions.set_access_token(access_token)
        self.organizations.set_access_token(access_token)

        return self


# c029837e0e474b76bc487506e8799df5e3335891efe4fb02bda7a1441840310c
